name: k42-landing-page-optimizer
version: 1.0.0
display_name: "Landing Page Optimizer"
description: >
  Analyzes landing page copy and structure, produces optimized versions with improved headlines, CTAs, and social proof.
author: NemoClaw Factory
created: 2026-03-31
language: en

family: K42
domain: K
tag: marketing
skill_type: generator

schema_version: 2
runner_version_required: ">=4.0.0"
routing_system_version_required: ">=3.0.0"
max_loop_iterations: 8

context_requirements:
  - workflow_id
  - budget_state
  - step_history

execution_role: >
  You are a conversion rate optimization expert. Analyze and optimize landing page elements for maximum conversion.

inputs:
  - name: page_content
    type: string
    required: true
    description: "Current landing page content and structure"
    validation:
      min_length: 20
      max_length: 15000
  - name: target_action
    type: string
    required: false
    description: "Desired conversion action"
    validation:
      min_length: 5
      max_length: 10000
    default: "sign_up"

outputs:
  - name: result
    type: string
    description: "The generated output"
  - name: result_file
    type: file_path
    description: "Path to the markdown artifact"
  - name: envelope_file
    type: file_path
    description: "Path to the JSON envelope for skill chaining"

artifacts:
  storage_location: skills/k42-landing-page-optimizer/outputs/
  filename_pattern: "k42-landing-page-optimizer_{workflow_id}_{timestamp}.md"
  envelope_pattern: "k42-landing-page-optimizer_{workflow_id}_{timestamp}_envelope.json"
  format: markdown
  committed_to_repo: false
  gitignored: true

steps:
  - id: step_1
    name: "Parse input and prepare analysis context"
    step_type: local
    task_class: general_short
    description: "Validate inputs, extract key parameters, prepare context for generation."
    input_source: inputs
    output_key: step_1_output
    failure:
      strategy: halt
      retry_count: 0

  - id: step_2
    name: "Generate primary output"
    step_type: llm
    task_class: premium
    description: "Core generation step using execution_role persona."
    input_source: step_1.output
    output_key: generated_output
    failure:
      strategy: retry
      retry_count: 2

  - id: step_3
    name: "Evaluate output quality"
    step_type: critic
    task_class: moderate
    description: "Score output on relevance, completeness, and actionability."
    input_source: step_2.output
    output_key: step_3_output
    failure:
      strategy: fallback
      retry_count: 1
      fallback_step: step_5

  - id: step_5
    name: "Validate and write artifact"
    step_type: local
    task_class: general_short
    description: "Write markdown artifact and JSON envelope."
    input_source: __final_output__
    output_key: artifact_path
    failure:
      strategy: halt
      retry_count: 0

routing:
  default_alias: premium
  allow_override: false

composable:
  output_type: "marketing_generator_output"
  can_feed_into: []
  accepts_input_from: []

metadata:
  generated_by: "skill-factory"
  factory_version: "E-5"
  quality_score: null
  approved_by: null

quality_gate:
  min_quality_score: 10.0
  retry_on_fail: true
  max_retries: 5
  escalate_below: 5.0

critic_loop:
  enabled: true
  min_score: 10.0
  max_iterations: 5
  escalation: retry_with_feedback
